startup-config   # saved config file that stored in NVRAM. Contains all commands
# that will be used with the load or reload. Will not lose it's data when power off.

running-config   #

show startup-config
show running-config